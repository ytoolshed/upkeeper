TODO:
  * tables for state:
      service actual_state desired_state
        UP/DOWN
  * table for cmd line parameters
  * triggers for updates

lowprio:
  * table for upkeeper daemon state (sleeping, processing)

DONE:
  * table for events (started, crashed, stopped)
