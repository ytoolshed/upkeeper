/*
 *       We should test coe by duping stderr to fd 3 and marking it close on exec
 *       then forking, execing and making sure a write returns -1 with errno == EBADF
 *       
 *       I guess.     
 */ 