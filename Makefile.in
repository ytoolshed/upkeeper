SHELL           = @SHELL@
CF_ALL	        = @CFLAGS@  -MD -I. 
LDFLAGS         = @LDFLAGS@
LDLIBS          =
LF_ALL		= @LDFLAGS@ -ldl
CC		= @CC@

.SUFFIXES:
.SUFFIXES: .c .o .tap .t 

COMP            = $(CC) $(CF_ALL) $(CF_TGT) -o $@ -c $<
LINK            = $(CC) $(CF_ALL) $(LF_TGT) -o $@ $^ $(LL_TGT) $(LL_DIR)
COMPLINK        = $(CC) $(CF_ALL) $(CF_TGT) $(LF_ALL) $(LF_TGT) -o $@ $< $(LL_TGT) 
ARCH		= $(AR) $(ARFLAGS) $@ $^


all:

AUTOCONF_LEFTOVERS=autom4te.cache autoscan.log config.h config.h.in~ config.log config.status */Makefile

DEPS_LEFTOVERS=*/*.dSYM

test-files:
	echo $(CHECK)

distclean: clean
	rm -rf $(AUTOCONF_LEFTOVERS) $(DEPS_LEFTOVERS) Makefile store/store.sqlite


Makefile: Makefile.in configure.ac 
	./config.status

all:	targets

include Rules.mk
